export enum TaskStatus {
	ONGOING = "ongoing",
	END = "end",
	EXPIRED = "expired"
}

export enum TaskType {
	COUNTDOWN = "countdown"
}

export enum ActiveItemType {
	INPUT = "el-input",
	CHECKBOX = "el-Checkbox"
}
